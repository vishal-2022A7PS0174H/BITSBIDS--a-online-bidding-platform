import { BlurUsernamePipe } from './blur-username.pipe';

describe('BlurUsernamePipe', () => {
  it('create an instance', () => {
    const pipe = new BlurUsernamePipe();
    expect(pipe).toBeTruthy();
  });
});
